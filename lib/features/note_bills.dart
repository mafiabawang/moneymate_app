import 'package:flutter/material.dart';

class NoteBills extends StatelessWidget {
  const NoteBills({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Note Bills'),
      ),
    );
  }
}